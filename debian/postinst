#!/bin/sh
set -e

git config --system --unset alias.ls
git config --system --add alias.ls "ls-files"

git config --system --unset alias.lso
git config --system --add alias.lso "ls-files --others --exclude-standard"

#DEBHELPER#
